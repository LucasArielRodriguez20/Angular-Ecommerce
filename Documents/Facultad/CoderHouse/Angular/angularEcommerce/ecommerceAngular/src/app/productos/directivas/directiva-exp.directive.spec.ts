import { DirectivaExpDirective } from './directiva-exp.directive';

describe('DirectivaExpDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaExpDirective();
    expect(directive).toBeTruthy();
  });
});
