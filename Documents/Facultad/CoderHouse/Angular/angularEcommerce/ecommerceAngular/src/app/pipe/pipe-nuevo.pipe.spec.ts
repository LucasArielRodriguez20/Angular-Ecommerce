import { PipeNuevoPipe } from './pipe-nuevo.pipe';

describe('PipeNuevoPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeNuevoPipe();
    expect(pipe).toBeTruthy();
  });
});
